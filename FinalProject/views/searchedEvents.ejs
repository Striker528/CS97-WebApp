<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Event Page</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>

  <body>
    <h1 data-position="header"> Searched Events </h1>

    <section data-position="events">
      <h2>Events</h2>
      <ul class="events">
        <!-- Loop through quotes -->
        <% for(var i = 0; i < events.length; i++) {%>
          <li class="event">
            <!-- Output name from the iterated quote object -->
            <span>Event: <%= events[i].Name %> </span> <br>
            <span>Host:   <%= events[i].Host %></span> <br>
            <span>Date:   <%= events[i].Date %></span> <br>
            <span>Time:  <%= events[i].Time %></span> <br>
            <span>Where:  <%= events[i].Where %></span> <br>
            <span>Details:<%= events[i].Description %></span> <br> <br>
          </li>
        <% } %>
      </ul>
    </section>


    <script src="/main.js"></script>
  </body>
</html>